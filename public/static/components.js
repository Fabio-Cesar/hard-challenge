// Nessa camada, constroi-se um componente de estrutura fixa mas que pode possuir informações variadas. Tem um método GET para retornar o HTML do elemento. Também vincula aos componentes datasets (data-****) relativos a sua função na alteração da estrutura da página, bem como qualquer id ou class necessário para a sua aquisição e utilização na página.

// export function Card() {

// async function getHTML() {
//    return ``;
// };

// return { getHTML };

// };